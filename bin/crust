#!/usr/bin/env bash
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
if [[ -x "$SCRIPT_DIR/crust-bin" ]]; then
  exec "$SCRIPT_DIR/crust-bin" "$@"
else
  exec bun "$SCRIPT_DIR/../src/index.ts" "$@"
fi
